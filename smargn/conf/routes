# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# CORS authorization
OPTIONS        /*url                       controllers.Application.options(url)
# Home page
GET            /                           controllers.Application.index

# Get blank page
GET            /naive/:word                controllers.Application.runNaive(word)

# Blank search page
GET            /naive                      controllers.Application.blankNaive
# Search for words
POST           /naive                      controllers.Application.naive

GET            /naive/inverse/:word        controllers.Application.runNaiveInverse(word)

GET            /naive/shift/:word          controllers.Application.runNaiveShift(word)

# Map static resources from the /public folder to the /assets URL path
GET            /assets/*file               controllers.Assets.at(path="/public", file)
